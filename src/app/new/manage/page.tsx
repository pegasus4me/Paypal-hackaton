'use client'

export default function Manage() {
    // I retrive all the streams by user address
    // here I use getStreamHash() function
    // I loop and I get the hashed
    // I use the mapping and I get the currents user streams
    return (
        <div>
            <p>manage</p>
        </div>
    )
}