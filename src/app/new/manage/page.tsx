'use client'
import Stream from "@/app/_components/Stream"
export default function Manage() {
    // I retrive all the streams by user address
    // here I use getStreamHash() function
    // I loop and I get the hashed
    // I use the mapping and I get the currents user streams
// User acquisition strategy and costs



    // cancel pause, unpause, update
    // manage hooks
    return (
        <div className="max-w-[60%] mx-auto border p-5 mt-10">
            <Stream/>
        </div>
    )
}